:root {
  color-scheme: dark;
  --bg: #030812;
  --ink: #f3fbff;
  --muted: #98aec7;
  --soft: #5f7694;
  --line: rgba(149, 209, 255, 0.18);
  --line-strong: rgba(122, 232, 255, 0.38);
  --panel: rgba(8, 18, 38, 0.72);
  --panel-2: rgba(12, 29, 61, 0.66);
  --blue: #2493ff;
  --cyan: #76f0ff;
  --violet: #8a7cff;
  --green: #65f3c4;
  --gold: #ffd87a;
  --radius-xl: 34px;
  --radius-lg: 24px;
  --radius-md: 16px;
  --max: 1180px;
}

* {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  margin: 0;
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  color: var(--ink);
  background:
    radial-gradient(circle at 16% -4%, rgba(46, 123, 255, 0.28), transparent 33rem),
    radial-gradient(circle at 88% 9%, rgba(0, 213, 255, 0.16), transparent 28rem),
    linear-gradient(180deg, #020611 0%, #06152e 47%, #030812 100%);
  overflow-x: hidden;
}

a {
  color: inherit;
  text-decoration: none;
}

button {
  font: inherit;
}

.ambient {
  position: fixed;
  pointer-events: none;
  z-index: -2;
  border-radius: 999px;
  filter: blur(28px);
}

.ambient-one {
  width: 22rem;
  height: 22rem;
  left: -7rem;
  top: 10rem;
  background: rgba(39, 145, 255, 0.22);
}

.ambient-two {
  width: 26rem;
  height: 26rem;
  right: -11rem;
  top: 28rem;
  background: rgba(101, 243, 196, 0.12);
}

.noise {
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: -1;
  opacity: 0.18;
  background-image:
    radial-gradient(circle, rgba(255,255,255,.42) 0 1px, transparent 1px),
    radial-gradient(circle, rgba(111,213,255,.3) 0 1px, transparent 1px);
  background-size: 180px 180px, 310px 310px;
  background-position: 0 0, 58px 96px;
  mask-image: linear-gradient(180deg, #000 0, transparent 78%);
}

.header {
  position: sticky;
  top: 0;
  z-index: 20;
  max-width: calc(var(--max) + 48px);
  margin: 0 auto;
  padding: 18px 24px;
  display: grid;
  grid-template-columns: auto 1fr auto;
  gap: 22px;
  align-items: center;
  transition: 180ms ease;
}

.header::before {
  content: "";
  position: absolute;
  inset: 8px 16px;
  z-index: -1;
  border: 1px solid rgba(149, 209, 255, .12);
  border-radius: 999px;
  background: rgba(3, 9, 20, .52);
  box-shadow: 0 14px 44px rgba(0,0,0,.24);
  backdrop-filter: blur(18px);
}

.header.scrolled::before {
  background: rgba(3, 9, 20, .78);
  border-color: rgba(149, 209, 255, .2);
}

.brand {
  display: inline-flex;
  align-items: center;
  gap: 11px;
  font-weight: 850;
  letter-spacing: -0.02em;
}

.brand img {
  width: 40px;
  height: 40px;
  border-radius: 13px;
  box-shadow: 0 0 26px rgba(36,147,255,.36);
}

.nav {
  justify-self: center;
  display: flex;
  gap: 6px;
}

.nav a {
  padding: 10px 12px;
  border-radius: 999px;
  color: var(--muted);
  font-size: .92rem;
  transition: 160ms ease;
}

.nav a:hover {
  color: var(--ink);
  background: rgba(255,255,255,.06);
}

.nav-cta,
.button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 44px;
  padding: 0 18px;
  border-radius: 999px;
  font-weight: 850;
  transition: 160ms ease;
}

.nav-cta,
.primary {
  color: #001526;
  background: linear-gradient(135deg, #ecfbff 0%, #76f0ff 42%, #2493ff 100%);
  box-shadow: 0 14px 36px rgba(36,147,255,.28);
}

.ghost {
  border: 1px solid var(--line);
  color: #dff8ff;
  background: rgba(255,255,255,.05);
}

.nav-cta:hover,
.button:hover {
  transform: translateY(-2px);
  border-color: var(--line-strong);
}

.menu {
  display: none;
  width: 44px;
  height: 44px;
  border: 1px solid var(--line);
  border-radius: 14px;
  background: rgba(255,255,255,.05);
}

.menu span {
  display: block;
  width: 18px;
  height: 2px;
  margin: 5px auto;
  border-radius: 999px;
  background: var(--ink);
}

.hero,
.section,
.footer {
  max-width: var(--max);
  margin: 0 auto;
  padding-left: 24px;
  padding-right: 24px;
}

.hero {
  min-height: calc(100vh - 78px);
  padding-top: 64px;
  padding-bottom: 84px;
  display: grid;
  grid-template-columns: minmax(0, .92fr) minmax(420px, 1.08fr);
  gap: 56px;
  align-items: center;
}

.soft-label,
.small-label,
.section-title p {
  display: inline-flex;
  align-items: center;
  gap: 9px;
  margin: 0 0 14px;
  color: var(--cyan);
  font-size: .78rem;
  font-weight: 900;
  letter-spacing: .08em;
  text-transform: uppercase;
}

.soft-label span {
  width: 8px;
  height: 8px;
  border-radius: 999px;
  background: var(--green);
  box-shadow: 0 0 18px rgba(101,243,196,.75);
}

h1,
h2,
h3,
p {
  margin-top: 0;
}

h1 {
  max-width: 760px;
  margin-bottom: 22px;
  font-size: clamp(3.1rem, 7.4vw, 6.5rem);
  line-height: .89;
  letter-spacing: -0.075em;
}

h2 {
  margin-bottom: 14px;
  font-size: clamp(2rem, 4.2vw, 4rem);
  line-height: 1;
  letter-spacing: -0.055em;
}

h3 {
  margin-bottom: 10px;
  font-size: 1.2rem;
  letter-spacing: -0.025em;
}

p,
.section-title span {
  color: var(--muted);
  line-height: 1.72;
}

.lead {
  max-width: 650px;
  font-size: 1.12rem;
  color: #c5d9ee;
}

.actions {
  display: flex;
  flex-wrap: wrap;
  gap: 13px;
  margin-top: 34px;
}

.trust-row {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-top: 34px;
}

.trust-row span {
  padding: 8px 11px;
  border: 1px solid rgba(149,209,255,.14);
  border-radius: 999px;
  color: #bdd3e9;
  background: rgba(255,255,255,.04);
  font-size: .84rem;
}

.hero-showcase {
  position: relative;
}

.app-frame {
  border: 1px solid rgba(149,209,255,.24);
  border-radius: 27px;
  overflow: hidden;
  background: rgba(3,9,20,.82);
  box-shadow:
    0 34px 120px rgba(0, 83, 255, .22),
    0 0 0 1px rgba(255,255,255,.04) inset,
    0 0 70px rgba(118,240,255,.08);
  transform: perspective(1200px) rotateY(-3deg) rotateX(1deg);
}

.window-bar {
  height: 42px;
  display: grid;
  grid-template-columns: 80px 1fr 80px;
  align-items: center;
  border-bottom: 1px solid rgba(149,209,255,.14);
  background: linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.015));
}

.traffic {
  display: flex;
  gap: 7px;
  padding-left: 18px;
}

.traffic i {
  width: 11px;
  height: 11px;
  border-radius: 999px;
  background: #ff5f57;
}

.traffic i:nth-child(2) { background: #febc2e; }
.traffic i:nth-child(3) { background: #28c840; }

.window-bar span {
  justify-self: center;
  color: #9eb5cf;
  font-size: .82rem;
  font-weight: 800;
}

.app-frame img {
  display: block;
  width: 100%;
  height: auto;
}

.showcase-card {
  position: absolute;
  padding: 13px 15px;
  border: 1px solid rgba(118,240,255,.22);
  border-radius: 18px;
  background: rgba(5,14,31,.76);
  box-shadow: 0 18px 44px rgba(0,0,0,.3);
  backdrop-filter: blur(14px);
}

.showcase-card strong {
  display: block;
  color: #eaffff;
}

.showcase-card span {
  color: var(--muted);
  font-size: .84rem;
}

.card-left {
  left: -24px;
  bottom: 40px;
}

.card-right {
  right: -18px;
  top: 62px;
}

.section {
  padding-top: 82px;
  padding-bottom: 82px;
}

.section-title {
  max-width: 760px;
}

.section-title.centered {
  max-width: 830px;
  text-align: center;
  margin: 0 auto 34px;
}

.intro {
  padding-bottom: 26px;
}

.feature-grid {
  padding-top: 18px;
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 18px;
}

.feature-card,
.flow-panel,
.infra-panel,
.future-card,
.repo-shell {
  border: 1px solid var(--line);
  border-radius: var(--radius-lg);
  background:
    linear-gradient(150deg, rgba(255,255,255,.075), rgba(255,255,255,.018)),
    rgba(7,17,37,.72);
  box-shadow: 0 22px 70px rgba(0,0,0,.24);
}

.feature-card {
  min-height: 260px;
  padding: 24px;
}

.icon {
  width: 58px;
  height: 58px;
  margin-bottom: 28px;
  border: 1px solid rgba(118,240,255,.22);
  border-radius: 18px;
  background:
    radial-gradient(circle at 40% 30%, rgba(118,240,255,.55), transparent 30%),
    linear-gradient(135deg, rgba(36,147,255,.26), rgba(138,124,255,.12));
  position: relative;
}

.icon::before,
.icon::after {
  content: "";
  position: absolute;
  inset: 16px;
  border: 2px solid rgba(229,250,255,.86);
  border-radius: 7px;
}

.icon.tree::before {
  border-radius: 999px 999px 6px 6px;
  transform: rotate(45deg);
}

.icon.layers::before {
  transform: translateY(-5px) rotate(45deg);
  border-radius: 5px;
}

.icon.layers::after {
  inset: 23px 14px 11px;
  border-width: 2px 0 0;
  border-radius: 0;
}

.icon.cloud::before {
  inset: 19px 12px 17px;
  border-radius: 999px;
}

.icon.cloud::after {
  inset: 24px 20px 19px;
  border-radius: 999px;
  transform: translateX(10px);
}

.flow-tabs {
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  gap: 10px;
  margin-bottom: 18px;
}

.flow-tab {
  border: 1px solid var(--line);
  border-radius: 999px;
  padding: 11px 16px;
  color: var(--muted);
  background: rgba(255,255,255,.04);
  cursor: pointer;
  transition: 160ms ease;
}

.flow-tab:hover,
.flow-tab.active {
  color: #001526;
  border-color: transparent;
  background: linear-gradient(135deg, #eaffff, #76f0ff, #2493ff);
}

.flow-panel {
  min-height: 230px;
  padding: 30px;
  display: grid;
  grid-template-columns: 96px 1fr 240px;
  align-items: center;
  gap: 24px;
}

.flow-number {
  width: 72px;
  height: 72px;
  display: grid;
  place-items: center;
  border: 1px solid var(--line-strong);
  border-radius: 24px;
  color: var(--cyan);
  font-size: 1.6rem;
  font-weight: 950;
  background: rgba(36,147,255,.12);
  box-shadow: 0 0 28px rgba(36,147,255,.18);
}

.flow-lines {
  display: grid;
  gap: 14px;
}

.flow-lines span {
  height: 12px;
  border-radius: 999px;
  background: linear-gradient(90deg, rgba(118,240,255,.14), rgba(118,240,255,.6), rgba(36,147,255,.16));
}

.flow-lines span:nth-child(2) {
  width: 72%;
}

.flow-lines span:nth-child(3) {
  width: 44%;
}

.infra-map {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 14px;
  margin-top: 30px;
}

.infra-node {
  min-height: 150px;
  padding: 20px;
  border: 1px solid var(--line);
  border-radius: 22px;
  color: var(--ink);
  background: rgba(255,255,255,.04);
  text-align: left;
  cursor: pointer;
  transition: 160ms ease;
}

.infra-node small {
  display: block;
  color: var(--cyan);
  font-weight: 950;
  margin-bottom: 24px;
}

.infra-node strong {
  display: block;
  margin-bottom: 8px;
}

.infra-node span {
  color: var(--muted);
}

.infra-node:hover,
.infra-node.active {
  border-color: var(--line-strong);
  background:
    radial-gradient(circle at 20% 10%, rgba(118,240,255,.18), transparent 46%),
    rgba(255,255,255,.055);
  transform: translateY(-2px);
}

.infra-panel {
  margin-top: 18px;
  padding: 28px;
}

.future-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 16px;
}

.future-card {
  padding: 24px;
  min-height: 220px;
}

.future-card:nth-child(1) { border-color: rgba(101,243,196,.25); }
.future-card:nth-child(2) { border-color: rgba(118,240,255,.24); }
.future-card:nth-child(3) { border-color: rgba(138,124,255,.28); }
.future-card:nth-child(4) { border-color: rgba(255,216,122,.24); }

.repo-shell {
  display: grid;
  grid-template-columns: .8fr 1.2fr;
  gap: 28px;
  padding: 34px;
}

.repo-list {
  display: grid;
  gap: 10px;
}

.repo-list a {
  display: grid;
  grid-template-columns: .8fr 1.2fr;
  gap: 16px;
  padding: 16px;
  border: 1px solid rgba(149,209,255,.12);
  border-radius: 18px;
  background: rgba(255,255,255,.035);
  transition: 160ms ease;
}

.repo-list a:hover {
  border-color: var(--line-strong);
  transform: translateX(3px);
}

.repo-list span {
  color: var(--muted);
}

.footer {
  padding-top: 24px;
  padding-bottom: 54px;
  display: flex;
  justify-content: space-between;
  gap: 28px;
  align-items: center;
  border-top: 1px solid rgba(149,209,255,.12);
}

.footer-brand {
  display: flex;
  align-items: center;
  gap: 14px;
}

.footer-brand img {
  width: 54px;
  height: 54px;
  border-radius: 16px;
}

.footer-brand strong,
.footer-brand span {
  display: block;
}

.footer-brand span,
.footer-links a {
  color: var(--muted);
  font-size: .92rem;
}

.footer-links {
  display: flex;
  flex-wrap: wrap;
  gap: 14px;
}

.footer-links a:hover {
  color: var(--cyan);
}

.reveal {
  opacity: 0;
  transform: translateY(22px);
  transition: opacity 720ms ease, transform 720ms ease;
}

.reveal.visible {
  opacity: 1;
  transform: translateY(0);
}

.delay-1 { transition-delay: 80ms; }
.delay-2 { transition-delay: 160ms; }
.delay-3 { transition-delay: 240ms; }

@media (max-width: 1020px) {
  .hero {
    grid-template-columns: 1fr;
  }

  .feature-grid,
  .infra-map,
  .future-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .flow-panel,
  .repo-shell {
    grid-template-columns: 1fr;
  }

  .flow-lines {
    display: none;
  }

  .card-left,
  .card-right {
    display: none;
  }
}

@media (max-width: 760px) {
  .header {
    grid-template-columns: auto 1fr auto;
  }

  .nav,
  .nav-cta {
    display: none;
  }

  .menu {
    display: block;
    justify-self: end;
  }

  .header.open .nav {
    position: fixed;
    left: 18px;
    right: 18px;
    top: 76px;
    display: grid;
    padding: 15px;
    border: 1px solid var(--line);
    border-radius: 24px;
    background: rgba(3,9,20,.95);
    backdrop-filter: blur(18px);
  }

  .hero,
  .section,
  .footer {
    padding-left: 18px;
    padding-right: 18px;
  }

  .hero {
    padding-top: 48px;
  }

  h1 {
    font-size: clamp(3rem, 17vw, 4.4rem);
  }

  .feature-grid,
  .infra-map,
  .future-grid {
    grid-template-columns: 1fr;
  }

  .app-frame {
    border-radius: 20px;
    transform: none;
  }

  .footer {
    align-items: flex-start;
    flex-direction: column;
  }

  .repo-list a {
    grid-template-columns: 1fr;
    gap: 4px;
  }
}
